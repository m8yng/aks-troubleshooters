apiVersion: v1
kind: Pod
metadata:
  name: aks-workload-identity-checker-pod
  namespace: default
  labels:
    azure.workload.identity/use: "true"
spec:
  containers:
  - name: aks-workload-identity-checker-container
    image: m8yng/aks-workload-identity-checker:latest # Swap registry if needed
    imagePullPolicy: Always
    env:
    - name: SDK_LANG
      value: "py" # py|go|rs
  serviceAccountName: workload-identity-sa # WI-enabled service account
  restartPolicy: Never # Run once, keep logs
